name: 'opiskelijavalinnan_raportointi'
version: '1.0.0'
config-version: 2

profile: 'opiskelijavalinnan_raportointi'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  opiskelijavalinnan_raportointi:
    +materialized: view
